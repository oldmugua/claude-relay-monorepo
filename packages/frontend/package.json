{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "cross-env NUXT_PUBLIC_API_BASE_URL=https://claude-back.901217.xyz/ nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npx wrangler pages deploy dist --project-name claude-relay-frontend"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.11.4",
    "@pinia/nuxt": "^0.5.1",
    "nuxt": "^4.0.1",
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
